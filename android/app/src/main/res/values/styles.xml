<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <!-- statusBarColor and navigationBarColor intentionally omitted: the app uses
         edge-to-edge (edgeToEdgeEnabled=true) so both bars are fully transparent and
         managed by WindowInsetsControllerCompat. Setting them explicitly here — even to
         transparent — triggers the deprecated Window.set/getStatusBarColor and
         Window.set/getNavigationBarColor APIs that Android 15 flags as incompatible
         with edge-to-edge mode. -->
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
</resources>